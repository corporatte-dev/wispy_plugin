local Types = require(script.Parent.Parent.Types)
local HUD = {} :: Types.HUD
HUD.NoMount = true

--[=[
    |> HUD System

    This should handle UI on the viewport such as notifications.
]=]

return HUD